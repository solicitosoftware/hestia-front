interface NamePathType {
  pathMain: string;
  pathCategories: string;
  pathProducts: string;
}

export const namePath: NamePathType = {
  pathMain: "/dashboard/main/",
  pathCategories: "dashboard/categories/",
  pathProducts: "dashboard/products/",
};
